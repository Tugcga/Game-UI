export enum CenterType {
    LEFT_TOP,
    LEFT_MIDDLE,
    LEFT_BOTTOM,
    CENTER_TOP,
    CENTER_MIDDLE,
    CENTER_BOTTOM,
    RIGHT_TOP,
    RIGHT_MIDDLE,
    RIGHT_BOTTOM
}

export enum UIType {
    ROOT,
    RECTANGLE,
    IMAGE,
    TEXT
}

export enum TextAlignType {
    LEFT_TOP,
    LEFT_MIDDLE,
    LEFT_BOTTOM,
    CENTER_TOP,
    CENTER_MIDDLE,
    CENTER_BOTTOM,
    RIGHT_TOP,
    RIGHT_MIDDLE,
    RIGHT_BOTTOM
}